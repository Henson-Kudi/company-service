// Implementation of all the providers defined in the application layer of the code.
// Example redisCache, jwtService, rabbitMq, passwordManager, etc
// Create a config folder to configure providers (services) that need configuration (ex: redis, rabbitMq)
